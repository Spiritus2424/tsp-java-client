# Ignore Gradle build ouput directory
build

# Ignore Gradle GUI config
gradle-app.setting

# Avoid ignoring Gradle Wrapper jar file (.jar files are usually ignored)
!gradle-wrapper.jar

# Cache of project
.gradle
